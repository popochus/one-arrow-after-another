# AIGC 协作开发日志

本文件用于**实时**记录开发过程中每一次借助 AIGC 工具完成的工作，作为博客
「AIGC 使用过程」一节的素材来源。作业要求记录应来自真实开发过程，因此每次
协作完成后立即追加，不做事后补写。

记录格式：

| 字段 | 说明 |
| --- | --- |
| 子任务 | 这一步在做什么 |
| 使用的工具 | 具体的 AIGC / Coding Agent 工具 |
| 提出的要求 | 我对 AI 提出的原始需求 |
| AI 产出 | AI 实际给出了什么 |
| 实际效果 | 拿去用了以后效果如何 |
| 人工修改 | 我自己做了什么判断和调整 |

---

## 记录 1 — 需求拆解与工程结构设计

- **子任务**：需求分析与代码结构设计
- **使用的工具**：WorkBuddy
- **提出的要求**：把作业要求（Python + 图形库实现"一箭又一箭"）转成落地的开发计划，
  并给出模块划分；同时指出作业里容易被扣分的点。
- **AI 产出**：
  1. 把作业硬性要求拆成了"界面 / 路径判断 / 碰撞失误 / 关卡流程 / AIGC 记录 / 测试 /
     GitHub / 博客"七个模块，并指出评分权重最高的是「箭头与路径判断」（25 分）。
  2. 指出两个扣分风险：一是"关卡实际上无法通关"（作业明确列为扣分项），二是
     "AIGC 记录事后虚构"。
  3. 据此提出用**求解器自动校验关卡可解性**的思路，并给出模块划分：
     `config`（配置）/ `board`（数据与路径检测）/ `levels`（关卡数据）/
     `solver`（求解与校验）/ `renderer`（绘制）/ `app`（主流程）。
- **实际效果**：结构清晰可用。将 `board.py` 独立出来后，求解器和测试都能直接复用
  同一套路径判断逻辑，避免了三处各写一遍导致口径不一致。
- **人工修改**：
  - 图形库选型上，AI 列出 Pygame / Tkinter / Pygame Zero 三个方案并说明取舍，
    我选了 **Pygame**（动画表现和后续打包 exe 的支持更好）。
  - 开发节奏上，AI 给出三个方案，我选了**最小可玩版本优先**：先用 1 个关卡把
    核心玩法跑通，确认效果后再扩展，出了问题也容易定位。
  - 保留 `solver.py` 的位置：它既是关卡校验工具，后续也能直接支撑「提示」和
    「自动求解」这类扩展功能。

---

## 记录 2 — 路径检测算法实现

- **子任务**：实现"箭头前方是否有阻挡"的核心判断
- **使用的工具**：WorkBuddy
- **提出的要求**：按作业给出的判定标准实现路径检测——只判断同一行或同一列、
  箭头与边界之间是否还有其他箭头；要处理边界情况。
- **AI 产出**：`Board.can_fly_out()`：从箭头下一格出发，沿朝向逐格前进，
  中途遇到箭头返回 False，走到棋盘外返回 True。用 `is_inside()` 统一做越界判断，
  把 4 个方向的边界处理收敛到一处。
- **实际效果**：逻辑正确。1×1 棋盘、贴边朝向棋盘外等情况都通过测试，未出现越界异常。
- **人工修改**：这一版代码没有改动，AI 一次就写对了。人工参与体现在需求界定和验收上：
  - 需求侧明确把范围卡在作业规定的"同一行 / 同一列到边界"，
    没有让 AI 顺手加斜向判断之类的额外规则；
  - 验收侧要求补边界用例后再采纳，最终用 9 项路径检测用例
    （1×1 棋盘、贴边朝外、阻挡不在同一行列等）逐条确认全部通过，
    用例保留在 `tools/run_tests.py` 里可复跑。

---

## 记录 3 — 测试失败的原因定位

- **子任务**：排查自动化测试中的 2 个失败用例
- **使用的工具**：WorkBuddy
- **提出的要求**：跑 `tools/run_tests.py` 后有 2 项 FAIL，帮忙判断是游戏逻辑错了
  还是测试写错了。
- **AI 产出**：逐项定位后确认**游戏逻辑没有问题，是测试用例本身写错了**：
  1. `P03` 用例想构造"朝上的箭头被上方箭头挡住"，但字符画 `"^\nv\n."` 里第 2 行的
     符号是 `v`（朝下）而不是 `^`，实际测的是另一个方向；
  2. `T05` 用例在调用 `restart()` **之后**才读取 `game.result`，而重开会把结果清空，
     断言必然失败。
- **实际效果**：修改测试用例后 15 项全部通过。
- **人工修改**：先确认"游戏逻辑没问题、是用例写错"这个结论成立——关键是顺着
  `on_click` → `restart` 的执行顺序看了一遍，确认 `result` 是在重开时被清空的，
  而不是被误判成失败状态丢失；确认无误后再执行修正：`P03/P04` 的字符画改为
  `"v\n^"` 和 `".\n^"`，让用例意图与符号一致；`T05` 把失败状态的读取移到
  `restart()` 之前。重跑后 15 项全部通过。

---

## 记录 4 — 界面三态改造与竖屏重排

- **子任务**：补齐开始界面 / 结果界面，并把界面从横屏改为手机竖屏
- **使用的工具**：WorkBuddy
- **提出的要求**：参考同学作业的界面完整度（开始页、通关页、失败页），把界面改成手机竖屏比例；
  范围严格控制在作业的基础要求内，不做附加玩法。
- **AI 产出**：
  1. `config.py` 窗口改为 720×1280，并把 HUD 区、棋盘区、按钮区的位置全部提为常量；
  2. `renderer.py` 新增开始界面与结果界面，抽出统一样式的 `draw_button()`；
  3. `app.py` 引入 `menu / playing / result` 三态机，点击按状态分发；
     通关或失败后先延迟 `FINISH_DELAY` 再切到结算界面，避免最后一支箭头的飞出动画被切掉。
- **实际效果**：三个界面均可正常进出，竖屏布局比原来的横屏更接近同类小游戏的观感；
  15 项自动化测试在适配状态机后全部通过。
  过程中 AI 的改动出过一处问题：用整批替换修改测试辅助函数时，把该函数自身的实现也一并
  替换掉了，触发 `RecursionError`（无限递归），定位后改回 `Game(get_screen())` 解决。
- **人工修改**：
  - 范围决策：明确**不做附加玩法**（提示 / 撤销 / 随机关卡），先把基础要求的 100 分拿满；
  - 界面比例决策：改为**手机竖屏**，因为作业参考的原作和同类游戏都是竖屏；
  - 验收：逐个确认开始、游戏中、通关、失败四个画面的文字与按钮位置正常。

---

## 记录 5 — 关卡扩充到 5 关，并保证每关都可通关

- **子任务**：把关卡从 1 关扩充到 5 关，且必须排除"设计了却通不了"的布局
- **使用的工具**：WorkBuddy
- **提出的要求**：再设计 4 个难度递增的关卡；作业把"关卡实际上无法通关"列为明确扣分项，
  所以要求给出一个能自动判断关卡是否有死锁的办法，而不是靠手工试。
- **AI 产出**：
  1. 先说明本游戏的规则推论：消除箭头只会减少障碍，"能否飞出"这个性质是单调的，
     所以只要反复消掉当前能飞的箭头，能清空就是可解、中途卡住就是死锁；据此写出了
     `tools/verify_levels.py`（已有）和 `game/solver.py` 的贪心求解；
  2. 新增 `tools/gen_levels.py`，采用**反向放置**构造可解布局——从最后一个被消除的箭头
     倒着往棋盘上放，放置时要求它到边界之间没有已放置的箭头，这样构造出的布局必然有解；
  3. 手工设计了前 3 关（教学关 → 纵向依赖 → 两端突破），后 2 关从生成器产出的候选中
     挑选箭头更多、依赖链更长的布局。
- **实际效果**：5 个关卡全部通过校验（8 / 10 / 10 / 14 / 14 个箭头），
  `tools/verify_levels.py` 输出"全部关卡均可通关"；生成器先产出的 4000 个布局经求解器
  复验全部可解，说明反向放置的推导成立。
- **人工修改**：
  - 难度曲线由人工定：前 3 关手工设计保证依赖关系一眼看得懂，后 2 关才上难度；
  - 关卡命名与主题由人工确定（顺藤摸瓜 / 层层推进 / 从两端突破 / 四面楚歌 / 天罗地网）；
  - 复核了生成器，发现候选里出现过"整块对称对峙"一类不好看的布局，最终挑选时避开。

---

## 记录 6 — 按作业要求补严测试用例

- **子任务**：核对测试项与作业要求是否一致，补上缺的断言
- **使用的工具**：WorkBuddy
- **提出的要求**：作业的 T04 原文是"消除本关全部箭头 → 显示通关**并进入下一关**"，
  但原用例只断言了"结果显示为 win"；另外要求验证多关卡流程整体能走通。
- **AI 产出**：
  1. 把 T04 补成两级断言：先确认本关清空且结果为 win，再调用结果界面的按钮，
     确认真的进入了第 2 关、且新关卡是完整初始状态（失误归零、箭头数满额）；
  2. 新增 T07：连续通关全部 5 关，并确认最后一关通关后不再存在"下一关"。
- **实际效果**：测试从 15 项增加到 16 项，全部通过；T07 输出"共 5 关全部通关，
  末关不再有下一关"。
- **人工修改**：确认了 T04 的判定顺序——必须先记录"本关已清空"再点按钮，
  否则按钮会把局面切到下一关，原先的状态就取不到了（这与记录 3 里 T05 踩过的坑是同一类）。

---

## 记录 7 — 按试玩反馈补上"返回主菜单"入口

- **子任务**：给游戏中和结算界面加上返回开始界面的途径
- **使用的工具**：WorkBuddy
- **提出的要求**：本人试玩 5 关后反馈——"进入游戏之后就没有按键可以回到主页面了，
  这点不好"。希望有一个能回到开始界面的操作。
- **AI 产出**：
  1. 键盘：把 `Esc` 改成**逐级返回**——游戏中和结算界面按 `Esc` 回开始界面，
     在开始界面按 `Esc` 才退出程序（原先 `Esc` 在任何界面都是直接退出）；
  2. 鼠标：底部原本只有一个「重新开始」按钮，改成**两个并排**——左边白底描边的
     「主菜单」（次要按钮），右边仍是蓝底主操作（重新开始 / 下一关 / 重试本关 / 再玩一次）；
  3. 开始界面加了一行提示"游戏中按 Esc 或点「主菜单」可返回这里"；
  4. 鼠标悬停判断同步排除次要按钮，避免指针停在按钮上时棋盘格子还在高亮。
- **实际效果**：Esc 和按钮两条路都能回到开始界面，回到开始界面后再开始游戏会从第 1 关
  的完整初始状态重新开始；新增的 T08 用例通过，测试总数 16 -> 17，全部通过。
- **人工修改**：
  - 需求由本人试玩后提出——原先只盯着作业的必做项，漏了"回主菜单"这个体验缺口；
  - 界面方案由 AI 给出后经确认：选择**底部双按钮并排**，而不是在顶部 HUD 塞一个小按钮，
    避免挡住关卡名；
  - 确认了 Esc 的层级语义（先返回、再退出），而不是直接改成"Esc 不再退出程序"。

---

## 记录 8 — 增加「继续游戏」与关卡选择界面

- **子任务**：在开始界面提供「继续游戏」，并加一个关卡选择界面
- **使用的工具**：WorkBuddy
- **提出的要求**：本人试玩后提出——"可以加个继续游戏，可以在开始界面点击开始游戏的时候
  加一个选择关卡的界面"。背景是：从游戏里返回主菜单后，只能从第 1 关重新开始。
- **AI 产出**：
  1. 状态机从三态扩到四态，新增 `select`（关卡选择界面）；
  2. 「开始游戏」不再直接开局，而是进入选关界面；选关卡片显示序号、主题名和状态
     （已通关打绿色标记，未通关显示箭头数量）；
  3. 用 `cleared_levels` 记录本次运行中通关过的关卡；由它推导出
     `resume_level`（第一个还没通关的关卡）和 `has_progress`；
  4. 通关过至少一关后，开始界面多出一个「继续游戏 · 第 N 关」按钮，直接续玩；
  5. 键盘同步适配：开始界面按 `Enter` 进选关界面，选关界面按 `Enter` 从待通关的关卡开始；
     `R` 键限定只在游戏中和结算界面生效（原先在选关界面也能触发重开）。
- **实际效果**：选关界面可任选 5 关中的任意一关直接开始；通关第 1 关后回到开始界面会出现
  「继续游戏 · 第 2 关」，点击直接进入第 2 关。测试 17 -> 19 项，全部通过。
- **人工修改**：
  - 两个功能都由本人试玩后提出，不是 AI 主动加的；
  - 交互方案经确认：选关界面**不做通关解锁限制**，任意关卡都能直接进
    （便于反复试玩和验证单关），"继续游戏"只作为便捷入口，不做成硬性进度门槛；
  - 进度记录暂存在内存中，关闭程序会重置；是否落盘保存留待后续决定。

---

## 记录 9 — 让自动化测试覆盖作业第 5 节，并产出可直接贴博客的报告

- **子任务**：按作业「5. 测试要求」把 T01 ~ T06 做成自动化测试，并解决"测试结果怎么写进博客"
- **使用的工具**：WorkBuddy
- **提出的要求**：本人把作业第 5 节的截图发过来，要求这一节**用自动化测试来解决就好**
  （作业原文允许"手工测试"或"编写自动化测试"二选一）。
- **AI 产出**：
  1. 先核对已有的 `tools/run_tests.py`，确认 T01 ~ T06 六项**早已实现**，
     但**测试内容和预期结果的字面表述与作业表格不一致**（例如 T02 原写作
     "箭头保留，失误 1"，作业原文是"箭头不消失，失误次数减 1"），
     照抄进博客会与要求对不上，于是把六项的字面表述全部改为与作业原文一致；
  2. 新增 `write_report()`：测试跑完后自动输出 `docs/test_report.md`，
     内含「编号 / 测试内容 / 预期结果 / 实际结果 / 是否通过」五列表格——
     正好对应作业"记录测试项目、预期结果、实际结果和是否通过"的要求，
     并把 T01 ~ T06 单独成节，补充用例另起一节；
  3. 顺手修掉报告里的两处可读性问题：`P06` 的编号被截断成 `P06-do`（改为完整方向名，
     测试内容也换成中文方向），`P05` 的"实际结果"只显示 `(True, True)`（改为文字描述）。
- **实际效果**：`python tools/run_tests.py` 一条命令跑完 **19 项全部通过**，
  并生成 `docs/test_report.md`；报告中 T01 ~ T06 的预期结果与作业表格逐字一致，
  实际结果一栏给出具体数字（如"剩余 8 → 7，失误 0"）而不是简单的 True/False。
- **人工修改**：
  - 决策由本人做出：这一节用**自动化测试**覆盖，不再另行手工测试（作业允许二选一）；
  - 代码改动均为 AI 完成，本次没有人工改代码；
  - 需注意：自动化测试替代的是作业第 5 节，第 1 节里"每个关卡都应由本人实际试玩"
    是独立要求，仍需人工完成，不能由测试脚本替代。

---

## 记录 10 — 配置提交身份，并把项目按功能拆成多次提交

- **子任务**：配置 git 提交身份；把已完成的代码拆成多次有意义的 commit
- **使用的工具**：WorkBuddy
- **提出的要求**：本人提供了 GitHub 账号绑定的邮箱，要求把项目提交上去。作业明确对
  "截止日期前一次性提交全部代码"扣分，所以需要拆成多次有意义的提交。
- **AI 产出**：
  1. 先澄清一处概念差异：GitHub 账号里绑定的邮箱是给网页端操作用的，和本地 git 的
     `user.name` / `user.email`（commit 记录里的署名）是两回事；本地此前两个值都是空的，
     不配置会直接导致 `git commit` 报错失败；
  2. 通过 GitHub API 查到账号 ID，推算出官方分配的隐私地址
     `249984935+popochus@users.noreply.github.com`（与 GitHub 设置页显示的一致），
     供本人选择；
  3. 提交前排查敏感信息：全仓库搜索 api key / token / password / cookie / 学号 等关键词，
     命中的 4 处均是代码注释里"字符画的 token"这一术语，没有实际密钥；
     同时把开发工具的工作数据目录 `.workbuddy/` 加入 `.gitignore`；
  4. 按功能模块拆成 **12 次提交**：工程骨架 → 箭头模型与路径检测 → 全局配置 → 关卡数据
     → 求解器与校验工具 → 渲染层 → 主流程与状态机 → 生成器与截图工具 → 自动化测试
     → 一键脚本 → 运行截图 → 开发日志。
- **实际效果**：12 条提交信息使用了作业推荐的前缀格式（`feat:` / `chore:` / `test:` / `docs:`）；
  `git ls-files` 共 27 个文件；工作区状态干净，`.workbuddy/` 未被纳入版本控制。
- **人工修改**：
  - 提交身份由本人决定：选择使用 GitHub 账号绑定的邮箱，而不是 noreply 隐私地址
    （已提示该邮箱会公开出现在每一条 commit 记录里）；
  - 仓库范围由 AI 界定：`.workbuddy/` 属于开发过程的工作数据、不是作业交付物，
    因此加入忽略清单，不随项目提交。

---

## 记录 11 — 远程仓库配置与推送（含代理与凭据问题排查）

- **子任务**：把本地仓库推送到 GitHub
- **使用的工具**：WorkBuddy
- **提出的要求**：把本地仓库推送到 GitHub。
- **AI 产出**：
  1. 配置远程 `origin` 指向 `https://github.com/popochus/one-arrow-after-another.git`；
  2. 直连 `github.com` 报 `schannel: server closed abruptly` 时，判断为 TLS 被中断，
     改用本机代理 127.0.0.1:7890 测试连通（HTTP 200），并把 `http.proxy` / `https.proxy`
     写入 git 全局配置；
  3. 首次推送卡死，通过 `tasklist` 发现执行的其实是 `git-credential-helper-sel`
     （交互式选择器）而非 GCM。定位到系统级配置 `etc/gitconfig` 里的
     `credential.helper = helper-selector` 排在最前，会等待终端输入；
  4. 清理被终止进程遗留的 `gitconfig.lock` → 移除系统级 helper-selector → 只保留
     `manager`，让凭据管理器以弹窗方式完成 OAuth 授权；
  5. 推送后用 GitHub API 独立复核：远程 `refs/heads/main` 的 SHA 与本地 HEAD 一致，
     13 条提交与 27 个文件均在位。
- **实际效果**：推送成功，`https://github.com/popochus/one-arrow-after-another` 可公开访问。
- **人工修改**：
  - 由本人完成 GitHub OAuth 授权（这是唯一必须人工参与的步骤）；
  - 仓库名、可见性（Public）、不勾选 README / .gitignore / License 三项，均由本人决定。

---

## 记录 12 — 编写 README.md

- **子任务**：按作业要求编写 README.md
- **使用的工具**：WorkBuddy
- **提出的要求**：按作业要求编写 README，需覆盖项目名称、游戏简介、开发环境、
  安装和运行方法、游戏操作说明、游戏截图六项内容。
- **AI 产出**：
  1. 先读取 `game/config.py`、`game/levels.py`、`requirements.txt`、`run_game.bat`
     核对真实参数，再据实撰写（如窗口 720×1280、5 关箭头数为 8/10/10/14/14、
     飞出 0.40 秒 / 晃动 0.34 秒 9 像素、失误上限 3 次），避免凭印象写错；
  2. 按六项要求组织章节，并补充「关卡可解性如何保证」「项目结构」「测试」三节，
     便于助教快速定位与复核；
  3. 截图以相对路径引用 `docs/screenshots/`，GitHub 上可直接渲染。
- **实际效果**：README 覆盖作业全部六项要求，并额外说明了关卡可解性的理论依据
  （单调性推论 + 反向放置生成器），与作业扣分项「关卡实际上无法通关」形成直接呼应。
- **人工修改**：
  - 隐私处理：仓库为 Public，按既定原则**不在 README 中写学号**；
  - 判断取舍：主动加入「开发说明」一节，如实声明开发过程借助 AIGC 工具，
    并指向 `DEVLOG.md`（作业本身要求使用 AI，如实声明比回避稳妥）；
  - 素材声明：明确项目不含任何第三方美术素材与音频，全部图形由代码实时绘制。

---

## 记录 13 — 据试玩反馈修复按键失效与动画速度

- **子任务**：修复本人试玩反馈的按键失效问题，并调整动画速度
- **使用的工具**：WorkBuddy
- **提出的要求**：试玩后反馈两点——①「按 R 没效果啊」；②飞出动画偏快，想看慢一点。
- **AI 产出**：
  1. **先验证再动手**：写临时脚本模拟三种场景（游戏中 / 结算界面 / 选关界面）派发
     `KEYDOWN` 事件，确认**按 R 的代码逻辑本身是正确的**——结算界面按 R 确实会把
     state 从 `result` 变回 `playing`、棋盘恢复 8/8。因此问题不在游戏逻辑，而是
     **按键事件没有送进程序**；
  2. 定位原因为 **Windows 中文输入法（IME）接管键盘**：SDL 默认在窗口创建时启用
     文本输入，输入法处于中文状态时 `R` 这类按键被用于组字，游戏收不到 `KEYDOWN`。
     修复：在 `main.py` 启动时调用 `pygame.key.stop_text_input()` 显式关闭；
  3. 同时增加一条**不依赖键盘的路径**：把底部按钮行改为按状态生成，通关结算时
     由两个按钮变为三个 —— 「主菜单 | 重玩本关 | 下一关」，鼠标也能重玩本关；
  4. 依试玩反馈把 `FLY_OUT_DURATION` 由 `0.40` 秒调为 `0.55` 秒。
- **实际效果**：**21 项自动化测试全部通过**（新增 T11 验证「重玩本关」按钮、
  T12 验证 R 键在游戏中与结算界面均生效）。截图确认三按钮布局正常、失败界面仍为两按钮。
- **人工修改**：
  - 问题由**本人实际试玩**发现，AI 未主动预见；
  - AI 的第一反应是"检查代码是否有 bug"，实测后才转向环境原因（输入法），
    说明「先复现、再定性」比直接改代码更有效；
  - 动画时长由本人给出主观判断（偏快），AI 只负责换算成具体参数。

---

## 记录 14 — 生成演示 GIF 并撰写博客正文

- **子任务**：给「项目展示」补一段演示动画；撰写博客正文
- **使用的工具**：WorkBuddy
- **提出的要求**：作业的博客部分要求"项目展示：展示开始界面、游戏过程、通关或失败界面，建议提供演示 GIF 或视频"，
  以及"AIGC 使用过程记录至少 3 次""测试结果表""PSP 表""心得体会"。要求按这些条目组织博客。
- **AI 产出**：
  1. **生成演示 GIF**：新增 `tools/make_gif.py`，用 SDL dummy 驱动离屏录制
     「开始界面 → 选关 → 游戏（含一次故意点错演示碰撞）→ 通关结算」共 107 帧，
     按 12 fps 采样、缩放至 360×640、统一调色板后输出 `docs/screenshots/demo.gif`，
     实测 8.7 秒 / 92 KB；
  2. **撰写博客正文**（`docs/blog.md`）：按作业要求的六个条目组织——项目展示（GIF + 6 张截图）、
     项目介绍（规则 / 界面 / 功能 / 特色）、实现思路（箭头与方向表示、路径检测、
     关卡表示与可解性校验，配 3 段关键代码）、AIGC 使用过程（13 条汇总表 + 3 条展开）、
     测试结果（T01~T06 + 12 项补充用例，数据取自 `docs/test_report.md`）、
     PSP 表、心得体会；
  3. **顺手修复 `DEVLOG.md` 自身的两处问题**：后半段 3 条记录的编号与前面重复
     （出现两个"记录 9""记录 10"），且字段名与前文不一致（"我的要求"vs"提出的要求"），
     已统一为 `## 记录 N — 标题` 与 6 个固定字段，编号改为 11 / 12 / 13。
- **实际效果**：GIF 生成并可正常播放（逐帧校验 52/52 帧对均有变化，总时长 8.7 秒）；
  博客正文覆盖作业全部条目，且所有数字均从代码或测试报告中核实（窗口 720×1280、
  飞出 0.55 秒、晃动 0.34 秒 / 9 像素、失误上限 3 次、关卡箭头数 8/10/10/14/14、测试 21 项）。
- **人工修改**：
  - **发现并改正了初稿里的一处虚构**：试玩记录原本写了"第 3 轮复验通过"，
    但该轮实际尚未进行（修复后的版本用户还没重新试玩），已改为"待复验"并说明原因；
    同理删掉了"演示 GIF 见仓库目录"这句当时还名不副实的话；
    （该轮试玩此后已完成，博客 5.4 中已按据实的确认口径重写，不再出现"待复验"字样）
  - **PSP 表的耗时数字为估算**，需本人按实际感受核对调整；
  - 隐私处理：博客顶部按班级实名制要求填了学号（与 GitHub 仓库不写学号的处理区分开）。

---

## 记录 15 — 新增「撤销」与「提示」（附加功能）

- **子任务**：从作业附加分项里挑两项实现——解谜辅助的「撤销上一步」与「提示」
- **使用的工具**：WorkBuddy
- **提出的要求**：基础功能已经完整，想再补一点附加分。在"提示 / 撤销 / 保存进度 / 星级计时 / 音效 / 打包 exe"里选做哪些，要求给出成本判断后再定。
- **AI 产出**：
  1. **先做成本判断**：指出 `game/solver.py` 当初就是按"供校验与提示复用"设计的，
     `hint()` 已经存在，所以提示功能是把已有能力接到界面上，**实现成本最低**；撤销次之；
     保存进度需要新增存档读写并让测试隔离存档路径，成本更高。据此建议先做这两项；
  2. **提示**：直接复用现成的 `hint()`。并补了一条推论写进注释——本游戏规则下消除只减少障碍，
     所以**只要关卡本身可解，任何时刻都至少有一个箭头能飞出去**，提示永远不会"提示不出来"；
  3. **撤销**：用**快照栈**实现，每次有效点击前把箭头布局与失误数存一份，撤销即恢复。
     棋盘最多 14 个箭头，复制开销可忽略，比设计一套"逆操作"逻辑更不易出错；
  4. **撤销时同步清空**飞出动画、晃动动画、提示高亮与结算判定，否则会出现
     "棋盘已经退回去了、箭头却还在飞"的错位；
  5. **界面**：底部改为两行——上行「撤销 / 提示」，下行「主菜单 / 重新开始」。
     没有可撤销步骤时「撤销」显示为禁用态、点不动；撤销后的提示用蓝色，
     与碰撞的红色区分开，避免两种反馈混淆；
  6. 一并加了键盘快捷键 `U`（撤销）、`H`（提示），与已有的 `R` 保持一致。
- **实际效果**：**21 → 26 项测试全部通过**。新增 T13（撤销已飞出的箭头）、T14（撤销一次失误，
  失误计数退还且撤销本身不消耗失误）、T15（提示高亮确实可飞出的箭头，点掉后自动收起）、
  T16（无历史时「撤销」置灰且点击无副作用）、T17（U / H 快捷键）。
- **人工修改**：
  - 附加功能**做哪些由本人选定**（在给出的成本排序里选了提示 + 撤销）；
  - 按钮布局最终采用两行而非挤成一行四个（720 宽放四个按钮过密），此为 AI 提出的方案；
  - 「撤销」需要禁用态、撤销提示要与碰撞提示区分颜色，是人工提出的表现要求。

---

## 记录 16 — 修复「窗口超出屏幕」：界面按屏幕自适应缩放

- **子任务**：修复本人截图反馈的显示问题——顶部文字被裁掉一半，且窗口拖不动
- **使用的工具**：WorkBuddy
- **提出的要求**：本人发了窗口截图，反馈"上面的文字显示不完全，并且不能拖动"。
- **AI 产出**：
  1. **先量化再动手**：用 Win32 `SPI_GETWORKAREA` 实测本机屏幕 1920×1080、
     去掉任务栏后可用工作区 **1920×1032**，而游戏窗口是 **720×1280**，**高出 248 像素**。
     Windows 放不下这么大的窗口就会把它整体上移 → 标题栏被顶到屏幕外 →
     **顶部文字被裁**与**没有标题栏可抓所以拖不动**其实是同一个根因，不是两个 bug；
  2. **对比两种取屏幕尺寸的途径**：Win32 API 返回 1920×1032（可信）；而 pygame 的
     `get_desktop_sizes()` 在无头驱动下返回假值 `(1024, 768)`，不能作为可靠来源
     —— 因此以 Win32 为准、pygame 兜底；
  3. **新增 `game/view.py`**：界面只认 720×1280 的"逻辑画布"，启动时按屏幕可用高度
     算出缩放比例（本机 0.7688，窗口变为 554×984，加上标题栏仍在 1032 之内），
     所有绘制照旧发生在逻辑画布上、最后整体缩放到窗口，鼠标坐标反向换算回逻辑坐标。
     **游戏逻辑与界面代码一行坐标都没改**；
  4. 屏幕足够大时保持 1:1，不放大（避免糊）。
- **实际效果**：回归 **26 项全部通过**（1:1 路径无变化）。缩放路径单独验证：
  用**窗口坐标**点击 8 个箭头格 **8/8 精确命中**，坐标往返误差 ≤ 1px；缩放模式下
  从开始界面完整走完一关，通关判定正常。**本人在第 5 轮试玩中复验通过**：
  关卡标题完整显示、标题栏可见且窗口能拖动。
- **人工修改**：
  - **本记录同时记下 AI 自身的一次失误**：改动过程中对同一个文件**并行发起多次编辑**，
    后写入的那次基于旧内容覆盖了前一次，导致 `app.py` 的构造函数改动丢失、
    `renderer.py` 少了一个 import。更值得记的是——**当时已有的 26 项测试完全没报错**，
    因为测试套件此前**从未调用过任何绘制函数**，只测逻辑与点击链路；
  - 据此补了 **T19 渲染冒烟**（四个界面各渲染一帧，用哨兵色判断有没有画满）。
    并做了反向验证：临时删掉那行 import 后 T19 立刻以 `NameError` 失败，旧套件抓不到；
  - 教训：**改了文件就要核对落盘结果**，以及**测试通过不等于没改坏**——要问一句"它到底覆盖到没有"。

---

## 记录 17 — 新增四声音效（全部代码合成，不引入音频文件）

- **子任务**：给几个关键事件加音效反馈
- **使用的工具**：WorkBuddy
- **提出的要求**：给游戏加音效。同时要求说明会引入什么依赖、要不要下载素材文件。
- **AI 产出**：先给了三条设计约束，再实现：
  1. **不引入外部音频素材**——README 里已声明"不使用第三方素材"，所以音效全部由代码合成正弦波形；
  2. **不引入新依赖**——pygame 生成波形通常走 `pygame.sndarray`，但它依赖 numpy，
     而项目只装了 pygame + pillow。改用 `pygame.mixer.Sound(buffer=...)` 直接喂原始 PCM 字节；
  3. **无声卡必须自动静音**——`mixer.init()` 在没有音频设备时会抛异常，整条音频链路做成
     "初始化失败就静默跳过"，否则在没声卡的机器上游戏会直接起不来；
  4. 新增 `game/audio.py`，四个音：飞出 620→1180 Hz 上扬 0.10 秒；碰撞 380→150 Hz 下坠 0.20 秒；
     撤销 660 / 520 Hz 两声 0.13 秒；通关 C-E-G 上行琶音 0.44 秒；
  5. **逐采样累加相位**，而不是每点直接算 `sin(2πft)`——后者在做滑音时相位会跳变，听感是"咔"的一声杂音；
  6. **惰性初始化**：import 本模块不做任何事，必须显式调用 `init()`，
     这样自动化测试、截图脚本与录制脚本都不会被动地打开音频设备；
  7. 通关时**只响通关音、不叠飞出音**（琶音本身已表达"完成"，再叠一个"嗖"只会让两段声音糊在一起）。
- **实际效果**：**28 → 29 项测试全部通过**（新增 T20 覆盖音效合成与无声卡降级）。
  波形自检：四个音**首尾采样均为 0**（起止无爆音）、**削波 0%**、峰值与设定振幅吻合。
  触发时机逐项验过：点掉箭头→`fly`、点错→`hit`、撤销→`undo`、最后一击→`win`。
- **人工修改**：**试听后反馈"有点刺耳"**，声学参数不是 AI 能自评的部分，见记录 18。

---

## 记录 18 — 修复「连点不结算」，并调柔音效

- **子任务**：处理试玩反馈的两个问题——① 音效刺耳；② 失误到上限后只要按得快就不会结算
- **使用的工具**：WorkBuddy
- **提出的要求**：本人试玩后反馈："有点刺耳"；并附截图显示失误已经 **11/3** 却仍未结算，
  "一直错误的话只要你按得快他就不会重新开始"。
- **AI 产出**：
  1. **定位连点问题**：`on_click` 在游戏进行中**没有检查胜负是否已判定**。判定失败后并不会
     立刻弹结算界面，而是先播完碰撞动画（`finish_delay` = 0.34 + 0.20 秒）。这段窗口里
     状态仍是「游戏中」，于是**每一次误点都把倒计时重置回满值**——点得越快，倒计时越走不完，
     结算界面永远不会出现。这与截图里"失误 11/3"的现象完全吻合；
  2. **顺带发现一个更严重的隐患**：判定失败后继续点击，如果把剩余箭头点光，会命中
     `remaining == 0` 的分支把 `result` 改写成 `"win"` —— **失败会被翻成胜利**。同一个根因带出来的；
  3. **修复**：在 `on_click` 里加一道输入锁，胜负一定就立即屏蔽棋盘与「撤销 / 提示」。
     这道锁的位置有讲究——必须放在**底部按钮行判断之后**，否则结算界面的「重试本关」
     会跟着一起失灵。同时撤掉格子高亮、把辅助按钮一并置灰，避免"亮着却点不动"的新困惑；
  4. **音效刺耳是三个原因叠加**：振幅 0.42 偏大；包络是对称正弦，0.1 秒的音**25 毫秒就到七成响度**
     （起音太陡）；频段偏高（飞出音收在 1180 Hz）。另外**同名音效可以同时播好几份**，
     快速连点时响度成倍叠加。逐项处理：振幅 0.42 → 0.24；包络改为前 14% 线性淡入 + 二次衰减；
     飞出音 620→1180 改为 480→780；通关音 523/659/784 改为 392/523/659；
     每个音效**独占一个声道**，新音打断旧音、不叠加。
- **实际效果**：量化对比——峰值 **-43%**、过零率（高频成分）**-20~31%**、有效响度约 **-8.5 dB**，
  首尾采样仍为 0（无爆音）。**29 → 30 项测试全部通过**，新增 T21（胜负判定后输入锁定，
  覆盖失败路径、胜利路径、结算按钮可用性三个子检查）。
  并按惯例做了反向验证：临时删掉那道锁后 T21 立即失败（29/30），其余 29 项不受影响。
- **人工修改**：
  - **两个问题都由本人实际试玩发现**，且第二个问题比表面看起来更严重（还带一个"败转胜"的隐患），
    这说明**高频操作去冲击"延迟切换状态"的窗口**是自动化测试容易漏掉的地方；
  - **"刺耳"是主观判断**，只能由本人给出，AI 负责量化成参数并验证改善幅度；
  - 调柔后的音量档位由本人试听后确认——第 5 轮试玩反馈"撤销和提示音效连点恢复现在没什么问题"，
    当前档位保留，不再调整。

---

## 记录 19 — 计时、星级评价与得分

- **子任务**：新增计分与评级系统——每关计时、按表现评星、算出并累计总分
- **使用的工具**：WorkBuddy
- **提出的要求**：本人提出"加得分计时和星级评价"。具体评级口径也由本人拍板：
  **星级看「失误次数 + 提示次数」，得分综合「失误与用时」**。
- **AI 产出**：
  1. **数据结构**：原先只有一个 `cleared_levels` 集合（只记"通关了没"），
     扩成 `records` 字典：关卡索引 → {星级, 得分, 用时, 失误次数, 提示次数}。
     `cleared_levels` 改为由 `records` 推导的只读属性——成绩表是唯一数据源，
     再存一份 set 就得时刻担心两者不同步；
  2. **计时**：`level_time` 只在「游戏进行中且胜负未定」时累加，结算界面、
     开始界面与选关界面都不走表，否则玩家赖在结算页也会被算进用时。
     胜负判定那一刻把用时定格到 `final_time`，之后的帧推进不再影响成绩；
  3. **星级**：惩罚点数 = 失误次数 + 提示次数，每 1 点降 1 星，下限 1 星
     （通关即有星；失败不评星）。**撤销不计入惩罚**——它退还的是失误计数，
     属于"走错了退回来"的容错机制；提示是主动向游戏要答案，故计入；
  4. **得分**：满分 1000，每次失误 −200，超出参考时长后每秒 −10，保底 200。
     参考时长 = 本关箭头数 × 5 秒——各关箭头数不同（8 ~ 14 个），
     若统一时长，后面几关会天然更容易超时；
  5. **重玩只保留更好的成绩**（先比星级，再比得分）：回头刷星有意义，
     也不会把已有的好成绩弄差；
  6. **界面**：HUD 第二行改为「剩余箭头 / 用时 / 失误」三项（用时夹在中间——
     它每帧都在变，放两侧会让另外两项跟着左右晃动）；结算界面画三颗星
     （用多边形自绘，不依赖字体里的 ★ 字形）＋ 本关得分 ＋ 成绩明细；
     选关卡片显示星级与得分；开始界面显示累计星数与总分；
     「提示」按钮上带本关已用次数，让玩家看得见用量。
- **实际效果**：测试 **30 → 34 项全通过**（新增 T22 计时、T23 星级评价、
  T24 得分、T25 判定后键盘 U / H 失效）；截图与演示动图按新界面全部重出。
- **人工修改**：
  - **评级口径由本人拍板**：AI 给的候选是"星级只看失误次数"或"星级看失误、
    得分看失误与用时"，本人改为 **星级同时计入提示次数**——理由是提示同样
    降低了游戏难度，不该完全免费（此前它既不影响成绩、也没有次数限制）；
  - **顺带修掉一个同源漏洞**：鼠标路径在 `on_click` 里已被"胜负已定"的守卫拦下，
    键盘路径（`U` / `H`）却没有这层守卫。后果是判定失败后按 `U` 能把 `result`
    清掉接着玩（"失误用尽即失败"形同虚设），按 `H` 则会白白多记一次提示、
    把星级拉低。两条路径的行为现已一致，并补 T25 专门覆盖。

---

## 记录 20 — 第 6 轮试玩：确认计时、星级与得分

- **子任务**：实机验证新增的计时、星级评价与得分三项功能
- **使用的工具**：WorkBuddy
- **提出的要求**：本人在实机试玩加入计分系统后的版本，给出结论"感觉不错了已经"；
  经追问确认，所指为**试玩了新的星级 / 得分 / 计时界面，三项均正常**
- **AI 产出**：按据实口径把本轮结论写入 `docs/blog.md` 5.4 试玩记录与本节。
  本轮本身不改动任何代码
- **实际效果**：三项功能实机表现正常，未发现新问题
- **人工修改**：无

---

（后续协作记录继续往下追加）
